python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"

ssh vm1@192.168.122.34
echo 'export ETL_ENCRYPTION_KEY="GENERATED_KEY' >> ~/.bashrc
source ~/.bashrc
exit

ssh vm2@192.168.122.231
echo 'export ETL_ENCRYPTION_KEY="GENERATED_KEY"' >> ~/.bashrc
source ~/.bashrc
exit


ssh-keygen -t ed25519 -C "your_email@example.com"
cat ~/.ssh/id_ed25519.pub
# Copy the output and add it to your GitHub account under Settings > SSH and GPG keys > New SSH key
git clone git@github.com:username/repo-name.git
cd repo-name
# Now you can use git commands like git pull and git push without entering your username and password


ssh vm1@192.168.122.34
# 1. Install Git, Python-pip, and Python-venv
sudo apt update
sudo apt install -y git python3-pip python3-venv

# 2. Clone your project from GitHub (replace with your actual repo URL)
git clone https://github.com/gerardpf/InfraETL1.git

# 3. Go into your project folder
cd InfraETL1

# 4. Create a virtual environment
python3 -m venv venv

# 5. Activate the environment
source venv/bin/activate

# 6. Install Python libraries for the Extractor
pip install requests cryptography

# 7. Log out of the VM
exit

ssh vm2@192.168.122.231

# 1. Install Git, Python-pip, and Python-venv
sudo apt update
sudo apt install -y git python3-pip python3-venv

# 2. Clone your project from GitHub (replace with your actual repo URL)
git clone https://github.com/gerardpf/InfraETL1.git

# 3. Go into your project folder
cd InfraETL1

# 4. Create a virtual environment
python3 -m venv venv

# 5. Activate the environment
source venv/bin/activate

# 6. Install Python libraries for the Transformer
pip install cryptography

# 7. Log out of the VM
exit

